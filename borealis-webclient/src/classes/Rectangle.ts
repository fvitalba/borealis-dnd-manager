class Rectangle {
    public x: number
    public y: number
    public width: number
    public height: number

    public constructor(newX: number, newY: number, newWidth: number, newHeight:number) {
        this.x = newX
        this.y = newY
        this.width = newWidth
        this.height = newHeight
    }
}

export default Rectangle

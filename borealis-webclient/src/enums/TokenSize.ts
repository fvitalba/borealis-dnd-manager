enum TokenSize {
    'tiny' = 10,
    'small' = 25,
    'medium' = 50,
    'large' = 75,
    'huge' = 100,
    'gargantuan' = 150,
}

export const TokenSizeArray = [TokenSize.tiny, TokenSize.small, TokenSize.medium, TokenSize.large, TokenSize.huge, TokenSize.gargantuan]

export default TokenSize
